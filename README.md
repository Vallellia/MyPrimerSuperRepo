# Pasos para el flujo basico de git
```sh
git status
git add . o git add -a
git status
git commit -m "descripcion del cambio"
git push
```

## Nota:
El mensaje del commit debe de ser una oracion descriptivo del cambio